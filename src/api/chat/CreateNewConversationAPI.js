function CreateNewConversationAPI(senderEmail, receiverEmail) {
    // TBC: create a new conversation with {members: [senderEmail, receiverEmail]}
    // API return: {status: whetherCreateSuccess}
    return true;
}

export default CreateNewConversationAPI;